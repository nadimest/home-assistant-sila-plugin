# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .camera_types import GrabSnapshot_Responses

if TYPE_CHECKING:

    from ...server import Server


class CameraBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        Demo feature: snaps JPEG frames at an adjustable digital zoom level.
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def GrabSnapshot(self, Zoom: float, *, metadata: MetadataDict) -> GrabSnapshot_Responses:
        """
        Takes a photo at the desired zoom level.


        :param Zoom: Multiplier for the digital zoom level (e.g. 1.0, 2.0).

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ImagePayload: The captured frame as JPEG-encoded bytes.


        """
